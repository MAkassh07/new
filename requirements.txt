Flask==3.0.3
Pillow==10.4.0
numpy==1.26.4
python-dotenv==1.0.1

# Optional — the app works without these (falls back to rule-based /
# pixel-heuristic engines). Install them when you're ready to go live:
# google-generativeai==0.8.3
# ultralytics==8.2.103
